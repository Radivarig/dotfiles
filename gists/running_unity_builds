unity > build > "fails with UnityPlayer.png missing"
unity > build settings > player settings > icon > set defult icon to none

$ cd project/Builds/<unity-build>
$ nix-shell -p steam-run-native
$ steam-run <unity-build>.x86_64 # fails
$ steam-run ./<unity-build>.x86_64 # works! but then sqlite3.so.0 cannot be found
$ export LD_LIBRARY_PATH="$LD_LIBRARY_PATH:path/to/sqlite/lib"
$ steam-run <unity-build>.x86_64 # now it works!

$ nix repl
> :b sqlite
  out: /path/to/sqlite/lib
> exit
